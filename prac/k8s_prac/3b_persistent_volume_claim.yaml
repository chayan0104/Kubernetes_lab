apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: apache-pvc
  namespace: apache-prod
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
  volumeName: apache-pv
